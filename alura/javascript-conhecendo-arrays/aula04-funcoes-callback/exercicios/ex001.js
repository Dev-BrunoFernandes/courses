// Utilize o método forEach para imprimir cada elemento de um array juntamente com seu índice.

const nomes = ['Lucas', 'Gabi', 'Iza', 'Nicolas'];

nomes.forEach((nome, indice) => {
    console.log(`Índice: ${indice}, Nome: ${nome}`);
});